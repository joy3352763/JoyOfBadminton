import SwiftUI

// TODO: Epic E1 — iPhoneScoreView
// 依 BDD 文件 §8 任務拆解實作，進入條件見 §9 銜接檢查點。

struct iPhoneScoreView: View {
    var body: some View {
        Text("iPhoneScoreView — Epic E1（待實作）")
    }
}
